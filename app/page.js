'use client';
import {useEffect,useState} from 'react';import {Sparkles,UserPlus,LogIn,ArrowRight,Phone,CalendarDays} from 'lucide-react';
export default function Home(){const [mode,setMode]=useState('home'),[form,setForm]=useState({firstName:'',lastName:'',phone:''}),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{let id=localStorage.getItem('libia_client_id');if(id)location.href=`/cliente?id=${id}`},[]);async function go(type){setBusy(true);setMsg('');let r=await fetch(type==='register'?'/api/register':'/api/lookup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(type==='register'?form:{phone:form.phone})});let d=await r.json();setBusy(false);if(!r.ok)return setMsg(d.error);localStorage.setItem('libia_client_id',d.client.id);location.href=`/cliente?id=${d.client.id}`}
return <main className="welcome"><div className="halo"/><header className="logoWrap"><img src="/libia-logo.jpg"/><span>LIBIA BEAUTY SALON</span></header>{mode==='home'?<section className="welcomeCard"><small>BEAUTY REWARDS</small><h1>Tu belleza merece <em>detalles especiales.</em></h1><p>Crea tu tarjeta de fidelidad o entra a la que ya tienes.</p><button className="goldBtn" onClick={()=>setMode('register')}><UserPlus/>Crear mi tarjeta<ArrowRight/></button><button className="softBtn" onClick={()=>setMode('login')}><LogIn/>Ya soy cliente</button><div className="mini"><Sparkles/> 5 visitas · recompensa especial</div></section>:<section className="welcomeCard formCard"><button className="back" onClick={()=>{setMode('home');setMsg('')}}>← Volver</button><small>{mode==='register'?'NUEVA CLIENTA':'BIENVENIDA DE NUEVO'}</small><h2>{mode==='register'?'Crea tu tarjeta':'Encuentra tu tarjeta'}</h2>{mode==='register'&&<><label>Nombre<input value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})}/></label><label>Apellido<input value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/></label></>}<label>Teléfono<input inputMode="tel" placeholder="(347) 000-0000" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label>{msg&&<p className="error">{msg}</p>}<button className="goldBtn" disabled={busy} onClick={()=>go(mode==='register'?'register':'login')}>{busy?'Un momento…':mode==='register'?'Crear mi tarjeta':'Entrar a mi tarjeta'}<ArrowRight/></button></section>}<footer>35-61 91st Street · Jackson Heights, NY <a href="/equipo">Uso interno</a></footer></main>}
