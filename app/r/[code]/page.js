'use client';
import {useEffect,useState} from 'react';import {Gift,ArrowRight,Sparkles} from 'lucide-react';
export default function Referral({params}){const [ref,setRef]=useState(null),[form,setForm]=useState({firstName:'',lastName:'',phone:''}),[msg,setMsg]=useState(''),[code,setCode]=useState('');useEffect(()=>{Promise.resolve(params).then(p=>{setCode(p.code);fetch(`/api/referral/${p.code}`).then(r=>r.json()).then(setRef)})},[params]);async function submit(){let r=await fetch('/api/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,referralCode:code})}),d=await r.json();if(!r.ok)return setMsg(d.error);localStorage.setItem('libia_client_id',d.client.id);location.href=`/cliente?id=${d.client.id}`}if(!ref)return <main className="loading">Preparando tu invitación…</main>;return <main className="refPage"><header><img src="/libia-logo.jpg"/><span>UNA INVITACIÓN ESPECIAL</span></header><section className="refBanner"><Sparkles/><small>LIBIA BEAUTY SALON</small><h1>{ref.referrer?.name?.split(' ')[0]||'Una amiga'} te invitó a consentirte.</h1><p>Regístrate y disfruta <b>5% de bienvenida</b> en Color, Highlights, Botox o Keratina.</p><div className="gift"><Gift/><span>Tu beneficio quedará guardado en tu nueva tarjeta.</span></div></section><section className="refForm"><h2>Crea tu tarjeta</h2><label>Nombre<input value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})}/></label><label>Apellido<input value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/></label><label>Teléfono<input inputMode="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label>{msg&&<p className="error">{msg}</p>}<button className="goldBtn" onClick={submit}>Crear mi tarjeta <ArrowRight/></button><small>El 5% se usa una sola vez en un servicio elegible.</small></section></main>}
