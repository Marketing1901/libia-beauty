'use client';
import {useEffect,useRef,useState} from 'react';import {ScanLine,Search,Settings,ShieldCheck,Scissors,Tag,Camera,User,Save,X} from 'lucide-react';
const eligible=['Color','Highlights','Botox','Keratina'];
export default function Equipo(){const [pin,setPin]=useState(''),[ok,setOk]=useState(false),[tab,setTab]=useState('counter'),[q,setQ]=useState(''),[results,setResults]=useState([]),[client,setClient]=useState(null),[data,setData]=useState(null),[service,setService]=useState('Color'),[msg,setMsg]=useState(''),[scan,setScan]=useState(false),[configPin,setConfigPin]=useState(''),[configOk,setConfigOk]=useState(false),[cfg,setCfg]=useState(null);useEffect(()=>{let id=new URLSearchParams(location.search).get('client');if(id)setClient({id})},[]);useEffect(()=>{if(client?.id)load(client.id)},[client]);async function load(id){let r=await fetch(`/api/client/${id}`),d=await r.json();setData(d);if(d.client)setClient(d.client)}async function search(){let r=await fetch('/api/staff',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin,query:q})}),d=await r.json();if(r.status===401)return setMsg('PIN incorrecto');setOk(true);setResults(d.clients||[])}async function action(type){setMsg('');let r=await fetch(`/api/staff/client/${client.id}/${type}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin,service})}),d=await r.json();setMsg(r.ok?(type==='visit'?'Visita registrada correctamente ✨':'Beneficio canjeado correctamente ✨'):d.error);if(r.ok)load(client.id)}if(!ok&&!client)return <main className="staffHome"><header><img src="/libia-logo.jpg"/><span>USO INTERNO</span></header><section className="staffGate"><ShieldCheck/><h1>Equipo Libia</h1><p>Acceso exclusivo para el counter.</p><label>PIN de operación<input type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)}/></label><button onClick={()=>{if(pin==='1901')setOk(true);else setMsg('PIN incorrecto')}}>Entrar</button>{msg&&<p className="error">{msg}</p>}</section></main>;if(tab==='settings')return <SettingsPanel pin={configPin} setPin={setConfigPin} access={configOk} setAccess={setConfigOk} cfg={cfg} setCfg={setCfg} back={()=>setTab('counter')}/>;return <main className="teamApp"><header className="teamHead"><img src="/libia-logo.jpg"/><div><small>LIBIA BEAUTY SALON</small><b>Counter</b></div></header><nav className="teamTabs"><button className="active" onClick={()=>{setClient(null);setData(null)}}><Search/>Clientes</button><button onClick={()=>setScan(true)}><ScanLine/>Escanear QR</button><button onClick={()=>setTab('settings')}><Settings/>Configuración</button></nav>{!client?<section className="counterStart"><small>OPERACIÓN</small><h1>¿A quién atendemos hoy?</h1><p>Escanea su QR personal o búscala por teléfono o nombre.</p><div className="searchBox"><Search/><input placeholder="Teléfono o nombre" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()}/><button onClick={search}>Buscar</button></div><button className="scanHero" onClick={()=>setScan(true)}><Camera/>Escanear QR de cliente</button><div className="results">{results.map(c=><button key={c.id} onClick={()=>setClient(c)}><User/><span><b>{c.name}</b><small>{c.phone}</small></span>›</button>)}</div></section>:<ClientPanel data={data} service={service} setService={setService} action={action} msg={msg} close={()=>{setClient(null);setData(null);setMsg('')}}/>}{scan&&<Scanner close={()=>setScan(false)} found={id=>{setScan(false);setClient({id})}}/>}</main>}
function ClientPanel({data,service,setService,action,msg,close}){if(!data?.client)return <section className="counterStart">Cargando cliente…</section>;let c=data.client;return <section className="clientRecord"><button className="back" onClick={close}>← Buscar otro cliente</button><span className="badge">CLIENTE</span><h1>{c.name}</h1><p>{c.phone}</p><div className="stats"><div><small>VISITAS</small><b>{data.visitCount}</b></div><div><small>REFERIDOS</small><b>{data.refPoints.toFixed(1)}</b></div></div><section className="counterCard"><h2>Registrar visita</h2><p>Cada visita suma a su tarjeta personal.</p><button className="darkAction" onClick={()=>action('visit')}><Scissors/>Registrar visita</button><hr/><h2>Beneficio de referido · 5%</h2><p>Solo Color, Highlights, Botox o Keratina.</p><div className="serviceButtons">{eligible.map(x=><button className={service===x?'selected':''} onClick={()=>setService(x)} key={x}>{x}</button>)}</div><button className="goldAction" onClick={()=>action('redeem')}><Tag/>Canjear 5% · {service}</button>{msg&&<div className="notice">{msg}</div>}</section></section>}
function Scanner({close,found}){const video=useRef(null),[err,setErr]=useState('');useEffect(()=>{let stream,timer;(async()=>{try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}}});video.current.srcObject=stream;await video.current.play();if(!('BarcodeDetector'in window)){setErr('Este navegador no permite lectura automática. Usa “Buscar cliente” como alternativa.');return}let det=new BarcodeDetector({formats:['qr_code']});timer=setInterval(async()=>{try{let a=await det.detect(video.current);if(a[0]){let u=new URL(a[0].rawValue);let id=u.searchParams.get('client');if(id)found(id)}}catch{}},500)}catch{setErr('No pudimos abrir la cámara. Revisa el permiso de cámara o busca al cliente por teléfono.')}})();return()=>{clearInterval(timer);stream?.getTracks().forEach(t=>t.stop())}},[]);return <div className="scanner"><div><button className="x" onClick={close}><X/></button><small>ESCÁNER LIBIA</small><h2>Escanea la tarjeta</h2><p>Apunta al QR personal de la clienta.</p><div className="camera"><video ref={video} playsInline muted/><span/></div>{err&&<p className="error">{err}</p>}</div></div>}
function SettingsPanel({pin,setPin,access,setAccess,cfg,setCfg,back}){const [msg,setMsg]=useState('');useEffect(()=>{if(access&&!cfg)fetch('/api/config').then(r=>r.json()).then(d=>setCfg(d.config))},[access]);if(!access)return <main className="staffHome"><section className="staffGate"><Settings/><h1>Configuración</h1><p>Esta área modifica el contenido global de Libia Beauty.</p><label>Código de configuración<input type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)}/></label><button onClick={()=>{if(pin==='190194')setAccess(true);else setMsg('Código incorrecto')}}>Abrir configuración</button>{msg&&<p className="error">{msg}</p>}<button className="back" onClick={back}>← Volver</button></section></main>;if(!cfg)return <main className="loading">Cargando configuración…</main>;async function save(){let r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin,...cfg})}),d=await r.json();setMsg(r.ok?'Cambios guardados para todos los dispositivos ✨':d.error)}return <main className="teamApp"><header className="teamHead"><img src="/libia-logo.jpg"/><div><small>CONFIGURACIÓN</small><b>Libia Beauty</b></div></header><section className="settingsPanel"><button className="back" onClick={back}>← Operación</button><h1>Contenido del salón</h1><p>Los cambios se aplican globalmente.</p>{[['visit_text','Texto · Mis visitas'],['ref_text','Texto · Mis referidos'],['ref_reward','Recompensa · Referidos'],['booking_label','Texto botón · Agendar'],['booking_url','URL destino · Agendar'],['contact_label','Texto botón · Contacto'],['contact_url','URL destino · Contacto']].map(([k,l])=><label key={k}>{l}<input value={cfg[k]||''} onChange={e=>setCfg({...cfg,[k]:e.target.value})}/></label>)}<button className="goldAction" onClick={save}><Save/>Guardar cambios</button>{msg&&<div className="notice">{msg}</div>}</section></main>}
